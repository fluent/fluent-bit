service:
    flush: 0.2
    log_level: error

pipeline:
    inputs:
        - name: event_type
          tag: event
          type: traces

    outputs:
        - name: stdout
          match: event
          processors:
            traces:
              - name: calyptia
                script: script.lua
                call: process_traces
