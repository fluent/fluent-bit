service:
  flush: 1
  grace: 1
  log_level: info
  http_server: on
  http_port: ${FLUENT_BIT_HTTP_MONITORING_PORT}

pipeline:
  inputs:
    - name: unix_socket
      socket_path: ${UNIX_SOCKET_PATH}
      socket_mode: ${UNIX_SOCKET_MODE}
  outputs:
    - name: null
      match: '*'
