service:
  flush: 12
  grace: 1
  log_level: info
  http_server: on
  http_port: ${FLUENT_BIT_HTTP_MONITORING_PORT}

pipeline:
  inputs:
    - name: elasticsearch
      listen: 0.0.0.0
      port: ${FLUENT_BIT_TEST_LISTENER_PORT}
      mem_buf_limit: 8KB
      http2: off
      tls: off

  outputs:
    - name: stdout
      match: '*'
