service:
  flush: 1
  grace: 1
  log_level: info
  http_server: on
  http_port: ${FLUENT_BIT_HTTP_MONITORING_PORT}

pipeline:
  inputs:
    - name: dummy
      alias: tag_records_disabled_dummy
      tag: tag.records.disabled
      dummy: '{"message":"disabled"}'
      samples: 1

  outputs:
    - name: stdout
      match: '*'
