service:
  flush: 1
  grace: 1
  log_level: error
  telemetry:
    metrics:
      logs:
        tag_records:
          # numeric booleans are restricted to 0/1; 2 must be rejected
          enabled: 2

pipeline:
  inputs:
    - name: dummy
      tag: bad.numbool
      dummy: '{"message":"x"}'
      samples: 1

  outputs:
    - name: 'null'
      match: '*'
